version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  generate-password:
    lang: node24
    handler: ./generate-password
    image: celerii/generate-password:latest

